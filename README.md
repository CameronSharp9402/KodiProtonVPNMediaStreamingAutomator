# KodiProtonVPNMediaStreamingAutomator
Python application designed to open and connect to a VPN via ProtonVPN, and once connected opens Kodi. Upon closing Kodi, the script will listen and kill the ProtonVPN instance, so that its only connected while streaming.
